{
  "name": "alien-invaders",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/update-meta.js && tsc -b && vite build",
    "preview": "vite preview",
    "deploy:prod": "bash scripts/deploy.sh"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.8"
  }
}
